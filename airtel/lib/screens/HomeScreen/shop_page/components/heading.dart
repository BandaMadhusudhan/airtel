import 'package:flutter/material.dart';
import 'package:flutter_screenutil/flutter_screenutil.dart';

class heading extends StatelessWidget {
  const heading({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return Text("buy new service",
    style: TextStyle(
     fontSize: 25.sp
    ),);
  }
}

